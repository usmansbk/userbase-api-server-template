type User {
  id: ID!
  firstName: String!
  lastName: String
  surname: String
  email: EmailAddress! @auth(rules: [{ allow: owner }])
  language: Locale!
  phoneNumber: PhoneNumber @auth(rules: [{ allow: owner }])
  isEmailVerified: Boolean!
  isPhoneNumberVerified: Boolean!
  socialPictureUrl: URL
  pictureUrl(edit: ImageEditInput): URL
  thumbnailUrl(edit: ImageEditInput): URL
  createdAt: DateTime!
  updatedAt: DateTime
  lastLoggedInAt: DateTime
  passwordLastUpdatedAt: DateTime
  phoneNumberLastUpdatedAt: DateTime
  emailLastUpdatedAt: DateTime
  pictureLastUpdatedAt: DateTime
}

type Query {
  me: User! @auth
}
